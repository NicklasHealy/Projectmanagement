// AuthProvider er ikke længere nødvendig med Power Automate-løsningen.
// Filen beholdes tom for kompatibilitet.
export default function AuthProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
